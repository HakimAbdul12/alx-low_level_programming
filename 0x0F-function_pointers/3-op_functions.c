/*******************op_add****************/
#include <stdlib.h>
/**
 * op_add - adds two numbers
 * @a: is the first number
 * @b: is the second number
 * Return: the results
 */
int op_add(int a, int b)
{
	return (a + b);
}

/*******************op_add****************/

/*******************op_add****************/
#include <stdlib.h>
/**
 * op_sub - subtract two numbers
 * @a: is the first number
 * @b: is the second number
 * Return: the results
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/*******************op_add****************/
#include <stdlib.h>
/**
 * op_mul - adds multiply numbers
 * @a: is the first number
 * @b: is the second number
 * Return: the results
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/*******************op_add****************/
#include <stdlib.h>
/**
 * op_div - divide two numbers
 * @a: is the first number
 * @b: is the second number
 * Return: the results
 */
int op_div(int a, int b)
{
	return (a / b);
}
/*******************op_add****************/
#include <stdlib.h>
/**
 * op_mod - find the remainder of two numbers
 * @a: is the first number
 * @b: is the second number
 * Return: the results
 */
int op_mod(int a, int b)
{
	return (a % b);
}

