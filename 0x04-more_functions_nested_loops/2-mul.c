#include "main.h"
#include <stdio.h>
/**
 * mul - muliplies two integers
 * @a: is an argument
 * @b: is also an input argument
 * Return: always (a * b)  success
 */
int mul(int a, int b)
{
	return (a * b);
}
